import { DEFAULT_ANIMATION_DELAY } from '../constants.js';

function getAnimationDelayForItems(count) {
  const scalingFactor = count * 0.1;
  return Math.min(DEFAULT_ANIMATION_DELAY / scalingFactor, DEFAULT_ANIMATION_DELAY);
}

export { getAnimationDelayForItems };
