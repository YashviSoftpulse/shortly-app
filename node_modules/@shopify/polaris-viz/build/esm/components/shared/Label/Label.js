import { HORIZONTAL_BAR_LABEL_HEIGHT, FONT_SIZE, FONT_FAMILY } from '@shopify/polaris-viz-core';
import { jsx } from 'react/jsx-runtime';

function Label({
  barHeight,
  color,
  label,
  labelWidth,
  y
}) {
  const labelYOffset = barHeight / 2;
  return /*#__PURE__*/jsx("text", {
    height: HORIZONTAL_BAR_LABEL_HEIGHT,
    width: labelWidth,
    "aria-hidden": "true",
    y: y + labelYOffset,
    fontSize: `${FONT_SIZE}px`,
    fontFamily: FONT_FAMILY,
    fill: color,
    dominantBaseline: "central",
    children: label
  });
}

export { Label };
