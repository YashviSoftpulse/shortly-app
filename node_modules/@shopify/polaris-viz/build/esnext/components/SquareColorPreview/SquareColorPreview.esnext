import { getCSSBackgroundFromColor } from '../../utilities/getCSSBackgroundFromColor.esnext';
import { PREVIEW_ICON_SIZE } from '../../constants.esnext';
import styles from './SquareColorPreview.scss.esnext';
import { jsx } from 'react/jsx-runtime';

const ANGLE = 305;
function SquareColorPreview({
  color
}) {
  const background = getCSSBackgroundFromColor(color, ANGLE);
  return /*#__PURE__*/jsx("span", {
    className: styles.ColorPreview,
    style: {
      background,
      height: PREVIEW_ICON_SIZE,
      width: PREVIEW_ICON_SIZE
    }
  });
}

export { SquareColorPreview };
