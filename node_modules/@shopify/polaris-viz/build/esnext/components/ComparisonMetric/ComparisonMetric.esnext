import { jsx } from 'react/jsx-runtime';
import { TrendIndicator } from '../TrendIndicator/TrendIndicator.esnext';

function ComparisonMetric({
  metric,
  trend,
  accessibilityLabel
}) {
  let direction;

  switch (trend) {
    case 'positive':
      direction = 'upward';
      break;

    case 'negative':
      direction = 'downward';
      break;

    case 'neutral':
    default:
      direction = undefined;
      break;
  }

  return /*#__PURE__*/jsx(TrendIndicator, {
    trend: trend,
    accessibilityLabel: accessibilityLabel,
    direction: direction,
    value: metric
  });
}

export { ComparisonMetric };
