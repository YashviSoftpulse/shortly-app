import { flattenDataGroupToDataSeries } from '../utilities/flattenDataGroupToDataSeries.esnext';
import { useThemeSeriesColors } from './useThemeSeriesColors.esnext';

function useThemeSeriesColorsForDataGroup(data, selectedTheme) {
  const series = flattenDataGroupToDataSeries(data);
  return useThemeSeriesColors(series, selectedTheme);
}

export { useThemeSeriesColorsForDataGroup };
