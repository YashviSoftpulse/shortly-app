'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var polarisVizCore = require('@shopify/polaris-viz-core');
var React = require('react');
var useWatchColorVisionEvents = require('../ColorVisionA11y/useWatchColorVisionEvents.js');

function useExternalHideEvents() {
  const [hiddenIndexes, setHiddenIndexes] = React.useState([]);
  useWatchColorVisionEvents.useWatchColorVisionEvents({
    type: polarisVizCore.EXTERNAL_EVENTS_SET_HIDDEN_ITEMS,
    onIndexChange: ({
      detail
    }) => {
      setHiddenIndexes(detail.indexes);
    }
  });
  return {
    hiddenIndexes
  };
}

exports.useExternalHideEvents = useExternalHideEvents;
