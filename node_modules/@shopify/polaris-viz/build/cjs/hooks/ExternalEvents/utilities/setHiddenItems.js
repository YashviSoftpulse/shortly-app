'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var polarisVizCore = require('@shopify/polaris-viz-core');
var utilities = require('../../ColorVisionA11y/utilities.js');

function setHiddenItems(id, indexes) {
  const custom = new CustomEvent(utilities.getEventName(id, polarisVizCore.EXTERNAL_EVENTS_SET_HIDDEN_ITEMS), {
    detail: {
      indexes
    }
  });
  window.dispatchEvent(custom);
}

exports.setHiddenItems = setHiddenItems;
