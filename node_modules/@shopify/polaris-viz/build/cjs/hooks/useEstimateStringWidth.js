'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var polarisVizCore = require('@shopify/polaris-viz-core');

function useEstimateStringWidth(string, fontSize = polarisVizCore.FONT_SIZE) {
  var _characterWidthOffset;

  const {
    characterWidths,
    characterWidthOffsets
  } = polarisVizCore.useChartContext();
  const offset = (_characterWidthOffset = characterWidthOffsets.fontSize[fontSize]) !== null && _characterWidthOffset !== void 0 ? _characterWidthOffset : 1;
  const width = polarisVizCore.estimateStringWidth(string, characterWidths);
  return width * offset;
}

exports.useEstimateStringWidth = useEstimateStringWidth;
