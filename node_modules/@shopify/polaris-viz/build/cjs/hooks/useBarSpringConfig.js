'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var polarisVizCore = require('@shopify/polaris-viz-core');

function useBarSpringConfig({
  animationDelay = 0
}) {
  const {
    shouldAnimate
  } = polarisVizCore.useChartContext();
  return polarisVizCore.useSpringConfig({
    animationDelay: shouldAnimate ? animationDelay : 0,
    shouldAnimate,
    unmountedSpringConfig: polarisVizCore.BARS_LOAD_ANIMATION_CONFIG,
    mountedSpringConfig: polarisVizCore.BARS_TRANSITION_CONFIG
  });
}

exports.useBarSpringConfig = useBarSpringConfig;
