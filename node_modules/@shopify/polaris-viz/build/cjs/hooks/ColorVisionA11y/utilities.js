'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var polarisVizCore = require('@shopify/polaris-viz-core');

function capitalize(string) {
  return string.charAt(0).toUpperCase() + string.slice(1);
}
function getDataSetItem(dataset, name) {
  return dataset[`${polarisVizCore.COLOR_VISION_EVENT.camelCaseName}${capitalize(name)}`];
}
function getEventName(id, type) {
  return `${id}:${polarisVizCore.COLOR_VISION_EVENT.name}:${type}`;
}

exports.capitalize = capitalize;
exports.getDataSetItem = getDataSetItem;
exports.getEventName = getEventName;
