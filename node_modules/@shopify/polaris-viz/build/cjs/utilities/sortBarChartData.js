'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

function sortBarChartData(labels, data) {
  return labels.map((_, index) => {
    return data.map(type => {
      var _type$data$index;

      const value = (_type$data$index = type.data[index]) === null || _type$data$index === void 0 ? void 0 : _type$data$index.value;

      if (value !== null && isNaN(Number(value))) {
        return null;
      }

      return value;
    });
  });
}

exports.sortBarChartData = sortBarChartData;
