'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var polarisVizCore = require('@shopify/polaris-viz-core');
var characterWidths = require('../data/character-widths.json.js');
var characterWidthOffsets = require('../data/character-width-offsets.json.js');

function estimateStringWidthWithOffset(string, fontSize, fontWeight) {
  var _characterWidthOffset, _characterWidthOffset2;

  const width = polarisVizCore.estimateStringWidth(string, characterWidths["default"]);
  const fontSizeOffset = (_characterWidthOffset = characterWidthOffsets["default"].fontSize[fontSize]) !== null && _characterWidthOffset !== void 0 ? _characterWidthOffset : 1;
  const fontWeightOffset = (_characterWidthOffset2 = characterWidthOffsets["default"].fontWeight[fontWeight]) !== null && _characterWidthOffset2 !== void 0 ? _characterWidthOffset2 : 1;
  const adjustedWidth = width * fontSizeOffset * fontWeightOffset;
  return Math.round((adjustedWidth + Number.EPSILON) * 100) / 100;
}

exports.estimateStringWidthWithOffset = estimateStringWidthWithOffset;
