'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var polarisVizCore = require('@shopify/polaris-viz-core');

function applyColorVisionToDomElement({
  element,
  activeIndex,
  isPerformanceImpacted
}) {
  const index = Number(element.dataset.index);
  const {
    opacity,
    transition
  } = polarisVizCore.getColorVisionStylesForActiveIndex({
    activeIndex,
    index
  });
  element.style.opacity = `${opacity}`;
  element.style.transition = isPerformanceImpacted ? 'none' : transition;
}

exports.applyColorVisionToDomElement = applyColorVisionToDomElement;
