'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var constants = require('../constants.js');

function shouldHideAnnotation({
  row,
  isShowingAllAnnotations,
  rowCount
}) {
  if (isShowingAllAnnotations) {
    return false;
  }

  if (rowCount === constants.COLLAPSED_ANNOTATIONS_COUNT) {
    return false;
  }

  if (rowCount > constants.COLLAPSED_ANNOTATIONS_COUNT && row > constants.COLLAPSED_ANNOTATIONS_COUNT - 1) {
    return true;
  }

  return false;
}

exports.shouldHideAnnotation = shouldHideAnnotation;
