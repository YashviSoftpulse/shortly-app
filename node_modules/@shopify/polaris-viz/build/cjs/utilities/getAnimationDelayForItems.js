'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var constants = require('../constants.js');

function getAnimationDelayForItems(count) {
  const scalingFactor = count * 0.1;
  return Math.min(constants.DEFAULT_ANIMATION_DELAY / scalingFactor, constants.DEFAULT_ANIMATION_DELAY);
}

exports.getAnimationDelayForItems = getAnimationDelayForItems;
