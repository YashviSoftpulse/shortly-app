'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var polarisVizCore = require('@shopify/polaris-viz-core');
var utilities = require('../hooks/ColorVisionA11y/utilities.js');

function setSingleSeriesActive(id, index) {
  const custom = new CustomEvent(utilities.getEventName(id, polarisVizCore.COLOR_VISION_SINGLE_ITEM), {
    detail: {
      index
    }
  });
  window.dispatchEvent(custom);
}

exports.setSingleSeriesActive = setSingleSeriesActive;
