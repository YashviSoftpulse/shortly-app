'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var getGapForIndex = require('../../../../utilities/getGapForIndex.js');

function getXPosition({
  start,
  end,
  seriesIndex,
  gaps,
  xScale
}) {
  if (start < 0) {
    return xScale(start) - getGapForIndex.getGapForIndex({
      gaps,
      direction: 'negative',
      seriesIndex
    });
  }

  if (end > 0) {
    return xScale(start) + getGapForIndex.getGapForIndex({
      gaps,
      direction: 'positive',
      seriesIndex
    });
  }

  return xScale(start);
}

exports.getXPosition = getXPosition;
