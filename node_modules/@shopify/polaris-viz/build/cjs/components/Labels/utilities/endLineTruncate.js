'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var truncateLastLine = require('./truncateLastLine.js');
var truncateSingleLine = require('./truncateSingleLine.js');
var polarisVizCore = require('@shopify/polaris-viz-core');

function endLineTruncate({
  label,
  targetWidth,
  targetHeight,
  characterWidths
}) {
  const isSingleLine = targetHeight <= polarisVizCore.LINE_HEIGHT;

  if (isSingleLine) {
    return truncateSingleLine.truncateSingleLine({
      label,
      targetWidth,
      characterWidths
    });
  }

  return truncateLastLine.truncateLastLine({
    label,
    targetWidth,
    targetHeight,
    characterWidths
  });
}

exports.endLineTruncate = endLineTruncate;
