'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var polarisVizCore = require('@shopify/polaris-viz-core');
var endWordTruncate = require('./endWordTruncate.js');
var truncateLabels = require('./truncateLabels.js');

const NEXT_INDEX = 1;
function getHorizontalLabels({
  labels,
  targetHeight,
  targetWidth,
  characterWidths
}) {
  const truncatedLabels = truncateLabels.truncateLabels({
    labels,
    targetWidth,
    targetHeight,
    characterWidths
  });
  const lines = [];
  let longestLineCount = 0;
  truncatedLabels.forEach((label, index) => {
    lines[index] = [];
    let line = '';
    let lineNumber = 0;
    const words = label.truncatedWords; // The reason we use a for loop here is we want
    // to be able to advance the loop below if we
    // are able to add multiple words to a line.
    // In that case, the while loop below will keep
    // skipping indexes until we need to break onto
    // another line.

    for (let wordIndex = 0; wordIndex < words.length; wordIndex++) {
      if (label.words[wordIndex] && label.words[wordIndex].wordWidth > targetWidth) {
        words[wordIndex] = endWordTruncate.endWordTruncate({
          word: words[wordIndex],
          targetWidth,
          characterWidths
        });
      }

      line += words[wordIndex];

      while (words[wordIndex + 1] != null && polarisVizCore.estimateStringWidth(`${line} ${words[wordIndex + NEXT_INDEX]}`, characterWidths) < targetWidth) {
        line += ` ${words[wordIndex + NEXT_INDEX]}`;
        wordIndex += 1;
      }

      lines[index].push({
        truncatedText: line,
        fullText: truncatedLabels[index].text,
        x: targetWidth / 2,
        y: lineNumber * polarisVizCore.LINE_HEIGHT,
        width: targetWidth,
        height: polarisVizCore.LINE_HEIGHT,
        textAnchor: 'middle',
        dominantBaseline: 'hanging'
      });
      lineNumber += 1;
      line = '';

      if (lineNumber > longestLineCount) {
        longestLineCount = lineNumber;
      }
    }
  });
  return {
    lines,
    containerHeight: longestLineCount * polarisVizCore.LINE_HEIGHT
  };
}

exports.getHorizontalLabels = getHorizontalLabels;
