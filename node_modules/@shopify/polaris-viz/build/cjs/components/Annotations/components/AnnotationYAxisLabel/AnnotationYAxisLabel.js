'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var polarisVizCore = require('@shopify/polaris-viz-core');
var constants = require('../../constants.js');
var useEstimateStringWidth = require('../../../../hooks/useEstimateStringWidth.js');
var jsxRuntime = require('react/jsx-runtime');

const FONT_SIZE = 10;
function AnnotationYAxisLabel({
  axis,
  label,
  x,
  y
}) {
  const selectedTheme = polarisVizCore.useTheme();
  const width = useEstimateStringWidth.useEstimateStringWidth(`${label}`, FONT_SIZE);
  const xOffset = axis === 'y2' ? 0 : width;
  return /*#__PURE__*/jsxRuntime.jsx("text", {
    width: width,
    height: constants.ANNOTATION_Y_AXIS_LABEL_HEIGHT,
    y: y,
    dominantBaseline: "middle",
    fontSize: FONT_SIZE,
    x: x - xOffset,
    fill: selectedTheme.annotations.axisLabelColor,
    children: label
  });
}

exports.AnnotationYAxisLabel = AnnotationYAxisLabel;
