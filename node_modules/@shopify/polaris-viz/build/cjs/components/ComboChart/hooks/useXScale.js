'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var React = require('react');
var d3Scale = require('d3-scale');
var useGetLabelsFromDataGroups = require('./useGetLabelsFromDataGroups.js');

function useXScale({
  drawableWidth,
  data,
  xAxisOptions
}) {
  const labels = useGetLabelsFromDataGroups.useGetLabelsFromDataGroups({
    data,
    xAxisOptions
  });
  const labelsLength = labels.length;
  const xScale = React.useMemo(() => {
    return d3Scale.scaleLinear().range([0, drawableWidth]).domain([0, labelsLength]);
  }, [drawableWidth, labelsLength]);
  return {
    xScale,
    labels
  };
}

exports.useXScale = useXScale;
