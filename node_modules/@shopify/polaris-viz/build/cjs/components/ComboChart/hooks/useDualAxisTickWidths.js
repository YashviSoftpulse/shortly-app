'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var polarisVizCore = require('@shopify/polaris-viz-core');
var React = require('react');
var getLongestTickWidth = require('../utilities/getLongestTickWidth.js');

function useDualAxisTicksWidth(leftTicks, rightTicks) {
  const {
    characterWidths
  } = polarisVizCore.useChartContext();
  return React.useMemo(() => {
    return {
      leftTickWidth: getLongestTickWidth.getLongestTickWidth(leftTicks, characterWidths),
      rightTickWidth: getLongestTickWidth.getLongestTickWidth(rightTicks, characterWidths)
    };
  }, [leftTicks, rightTicks, characterWidths]);
}

exports.useDualAxisTicksWidth = useDualAxisTicksWidth;
