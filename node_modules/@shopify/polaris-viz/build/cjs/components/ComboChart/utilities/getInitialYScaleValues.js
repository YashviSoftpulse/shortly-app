'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

function getInitialYScaleValues({
  drawableHeight,
  primaryAxis,
  shouldPlaceZeroInMiddleOfChart
}) {
  if (shouldPlaceZeroInMiddleOfChart) {
    return {
      drawableHeight,
      max: primaryAxis.max,
      min: -primaryAxis.max
    };
  }

  return {
    drawableHeight,
    max: primaryAxis.max,
    min: primaryAxis.min
  };
}

exports.getInitialYScaleValues = getInitialYScaleValues;
