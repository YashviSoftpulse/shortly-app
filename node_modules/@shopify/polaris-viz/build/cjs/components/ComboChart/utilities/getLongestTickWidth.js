'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var polarisVizCore = require('@shopify/polaris-viz-core');

function getLongestTickWidth(ticks, characterWidths) {
  return ticks.reduce((previous, {
    formattedValue
  }) => {
    const width = polarisVizCore.estimateStringWidth(formattedValue, characterWidths);

    if (width > previous) {
      return width;
    }

    return previous;
  }, 0);
}

exports.getLongestTickWidth = getLongestTickWidth;
