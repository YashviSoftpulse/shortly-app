'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var getGapForIndex = require('../../../utilities/getGapForIndex.js');

function getYPosition({
  start,
  end,
  groupIndex,
  gaps,
  yScale
}) {
  if (start < 0) {
    return yScale(end) + getGapForIndex.getGapForIndex({
      gaps,
      direction: 'negative',
      seriesIndex: groupIndex
    });
  }

  if (end > 0) {
    return yScale(end) - getGapForIndex.getGapForIndex({
      gaps,
      direction: 'positive',
      seriesIndex: groupIndex
    });
  }

  return 0;
}

exports.getYPosition = getYPosition;
