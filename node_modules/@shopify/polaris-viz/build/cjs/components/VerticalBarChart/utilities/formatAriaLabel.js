'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

function formatAriaLabel(series) {
  return `${series.title}: ${series.data.map(({
    label,
    value
  }) => `${label} ${value}`)}`;
}

exports.formatAriaLabel = formatAriaLabel;
