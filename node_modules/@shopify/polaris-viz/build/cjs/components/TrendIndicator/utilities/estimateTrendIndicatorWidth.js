'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var constants = require('../constants.js');
var estimateStringWidthWithOffset = require('../../../utilities/estimateStringWidthWithOffset.js');

function estimateTrendIndicatorWidth(value, fontWeight = constants.FONT_WEIGHT) {
  const textWidth = value ? estimateStringWidthWithOffset.estimateStringWidthWithOffset(value, constants.FONT_SIZE, fontWeight) : constants.NO_VALUE_WIDTH;
  const totalWidth = Math.round(constants.ICON_SIZE + constants.TEXT_ICON_SPACING + textWidth);
  return {
    totalWidth,
    textWidth
  };
}

exports.estimateTrendIndicatorWidth = estimateTrendIndicatorWidth;
