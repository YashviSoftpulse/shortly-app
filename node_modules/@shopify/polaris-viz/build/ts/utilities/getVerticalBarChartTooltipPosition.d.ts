import type { TooltipPosition, TooltipPositionParams } from '../components';
interface Props {
    tooltipPosition: TooltipPositionParams;
    chartXPosition: number;
    step: number;
    maxIndex: number;
    yMin: number;
    yMax: number;
    formatPositionForTooltip: (index: number) => TooltipPosition;
}
export declare function getVerticalBarChartTooltipPosition({ chartXPosition, formatPositionForTooltip, maxIndex, step, tooltipPosition, yMax, yMin, }: Props): TooltipPosition;
export {};
//# sourceMappingURL=getVerticalBarChartTooltipPosition.d.ts.map