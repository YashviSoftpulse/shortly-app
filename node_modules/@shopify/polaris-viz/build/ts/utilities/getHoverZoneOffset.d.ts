export interface Props {
    barSize: number;
    zeroPosition: number;
    max: number;
    position: 'vertical' | 'horizontal';
}
export declare function getHoverZoneOffset({ barSize, zeroPosition, max, position, }: Props): {
    clampedSize: number;
    offset: number;
};
//# sourceMappingURL=getHoverZoneOffset.d.ts.map