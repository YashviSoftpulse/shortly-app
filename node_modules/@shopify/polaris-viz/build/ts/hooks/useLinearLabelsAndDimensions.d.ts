import type { DataSeries } from '@shopify/polaris-viz-core';
interface Props {
    data: DataSeries[];
    drawableWidth: number;
    hideXAxis: boolean;
    labels: string[];
    longestSeriesLength: number;
}
export declare function useLinearLabelsAndDimensions({ data, drawableWidth: initialDrawableWidth, hideXAxis, labels, longestSeriesLength, }: Props): {
    labels: string[];
    xScale: import("d3-scale").ScaleLinear<number, number, never>;
    xAxisDetails: {
        labelWidth: number;
        reducedLabelIndexes: number[];
    };
};
export {};
//# sourceMappingURL=useLinearLabelsAndDimensions.d.ts.map