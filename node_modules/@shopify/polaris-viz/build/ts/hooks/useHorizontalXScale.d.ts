import type { LabelFormatter } from '@shopify/polaris-viz-core';
interface Props {
    allNumbers: number[];
    isStacked: boolean;
    maxWidth: number;
    stackedMax: number;
    stackedMin: number;
    labelFormatter: LabelFormatter;
    longestLabel: {
        positive: number;
        negative: number;
    };
}
export declare function useHorizontalXScale({ allNumbers, isStacked, labelFormatter, maxWidth, stackedMax, stackedMin, longestLabel, }: Props): {
    xScale: import("d3-scale").ScaleLinear<number, number, never>;
    ticks: number[];
    ticksFormatted: string[];
    drawableWidth: number;
    chartXPosition: number;
};
export {};
//# sourceMappingURL=useHorizontalXScale.d.ts.map