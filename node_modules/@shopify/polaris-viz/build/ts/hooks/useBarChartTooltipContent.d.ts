import type { ReactNode } from 'react';
import type { Color, DataSeries, LabelFormatter } from '@shopify/polaris-viz-core';
import type { RenderTooltipContentData } from '../types';
export interface Props {
    data: DataSeries[];
    seriesColors: Color[];
    renderTooltipContent: (data: RenderTooltipContentData) => ReactNode;
    seriesNameFormatter: LabelFormatter;
}
export declare function useBarChartTooltipContent({ data, renderTooltipContent, seriesColors, seriesNameFormatter, }: Props): (activeIndex: number) => ReactNode;
//# sourceMappingURL=useBarChartTooltipContent.d.ts.map