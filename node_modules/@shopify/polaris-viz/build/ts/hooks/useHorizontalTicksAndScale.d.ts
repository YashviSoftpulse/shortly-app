import type { LabelFormatter } from '@shopify/polaris-viz-core';
interface Props {
    allNumbers: number[];
    isStacked: boolean;
    labelFormatter: LabelFormatter;
    maxWidth: number;
    stackedMax: number;
    stackedMin: number;
}
export declare function useHorizontalTicksAndScale({ maxWidth, allNumbers, labelFormatter, isStacked, stackedMin, stackedMax, }: Props): {
    ticks: number[];
    xScale: import("d3-scale").ScaleLinear<number, number, never>;
    ticksFormatted: string[];
};
export {};
//# sourceMappingURL=useHorizontalTicksAndScale.d.ts.map