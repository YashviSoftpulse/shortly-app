/// <reference types="react" />
import type { Color, Dimensions, DataGroup, Direction, LabelFormatter } from '@shopify/polaris-viz-core';
import type { LegendData } from '../../../types';
export interface Props {
    showLegend: boolean;
    data: DataGroup[];
    seriesNameFormatter: LabelFormatter;
    colors?: Color[];
    dimensions?: Dimensions;
    direction?: Direction;
    maxWidth?: number;
}
export declare function useLegend({ colors, data, dimensions, showLegend, direction, maxWidth, seriesNameFormatter, }: Props): {
    legend: LegendData[];
    height: number;
    width: number;
    setLegendDimensions: import("react").Dispatch<import("react").SetStateAction<{
        height: number;
        width: number;
    }>>;
    isLegendMounted: boolean;
};
//# sourceMappingURL=useLegend.d.ts.map