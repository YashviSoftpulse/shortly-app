import type { Dispatch, SetStateAction } from 'react';
import type { ScaleBand, ScaleLinear } from 'd3-scale';
interface XAxisProps {
    allowLineWrap: boolean;
    labels: string[];
    labelWidth: number;
    onHeightChange: Dispatch<SetStateAction<number>>;
    x: number;
    xScale: ScaleLinear<number, number> | ScaleBand<string>;
    y: number;
    ariaHidden?: boolean;
    isLinearChart?: boolean;
    reducedLabelIndexes?: number[];
}
export declare function XAxis({ allowLineWrap, ariaHidden, isLinearChart, labels, labelWidth, onHeightChange, reducedLabelIndexes, x, xScale, y, }: XAxisProps): JSX.Element;
export {};
//# sourceMappingURL=XAxis.d.ts.map