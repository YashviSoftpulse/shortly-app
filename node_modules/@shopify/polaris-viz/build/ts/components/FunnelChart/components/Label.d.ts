import type { ReactNode } from 'react';
import type { SpringValue } from '@react-spring/web';
declare type Size = 'small' | 'large';
export interface LabelProps {
    label: string;
    labelWidth: number;
    labelHelper?: ReactNode;
    size: Size;
    color?: string;
    backgroundColor?: string;
    transform: SpringValue;
}
export declare function Label({ label, labelWidth, labelHelper, size, color, backgroundColor, transform, }: LabelProps): JSX.Element;
export {};
//# sourceMappingURL=Label.d.ts.map