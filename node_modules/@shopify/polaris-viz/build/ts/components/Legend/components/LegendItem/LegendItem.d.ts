import type { LabelFormatter } from '@shopify/polaris-viz-core';
import type { ReactNode } from 'react';
import type { LegendData } from '../../../../types';
export interface LegendItemDimension {
    width: number;
    height: number;
}
export declare const MINIMUM_LEGEND_ITEM_WIDTH = 100;
export interface LegendItemProps extends LegendData {
    index: number;
    activeIndex?: number;
    colorVisionType?: string;
    renderSeriesIcon?: () => ReactNode;
    theme?: string;
    onDimensionChange?: (index: number, { width, height }: LegendItemDimension) => void;
    backgroundColor?: string;
    truncate?: boolean;
    showLegendValues?: boolean;
    seriesNameFormatter?: LabelFormatter;
}
export declare function LegendItem({ activeIndex, color, colorVisionType, index, isComparison, name, renderSeriesIcon, shape, theme, value, onDimensionChange, backgroundColor, truncate, showLegendValues, seriesNameFormatter, }: LegendItemProps): JSX.Element;
//# sourceMappingURL=LegendItem.d.ts.map