import type { AnnotationPosition } from '../../../../Annotations';
import type { Annotation } from '../../../../../types';
export interface Props {
    annotations: Annotation[];
    dataIndexes: {
        [key: string]: number;
    };
    drawableWidth: number;
    groupHeight: number;
    zeroPosition: number;
}
export declare function useHorizontalBarChartYAnnotationsPositions({ annotations, dataIndexes, groupHeight, drawableWidth, zeroPosition, }: Props): {
    positions: AnnotationPosition[];
};
//# sourceMappingURL=useHorizontalBarChartYAnnotationsPositions.d.ts.map