import type { ScaleLinear } from 'd3-scale';
import type { AnnotationPosition } from '../../../../Annotations';
import type { Annotation } from '../../../../../types';
export interface Props {
    annotations: Annotation[];
    drawableWidth: number;
    isShowingAllAnnotations: boolean;
    onHeightChange: (height: number) => void;
    xScale: ScaleLinear<number, number>;
}
export declare function useHorizontalBarChartXAnnotationPositions({ annotations, drawableWidth, isShowingAllAnnotations, onHeightChange, xScale, }: Props): {
    hiddenAnnotationsCount: number;
    positions: AnnotationPosition[];
    rowCount: number;
};
//# sourceMappingURL=useHorizontalBarChartXAnnotationPositions.d.ts.map