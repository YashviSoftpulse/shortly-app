/// <reference types="react" />
import type { Color, Direction } from '@shopify/polaris-viz-core';
import type { Size } from '../../types';
interface Props {
    activeIndex: number;
    index: number;
    scale: number;
    color: Color;
    size: Size;
    direction: Direction;
    roundedCorners: number;
}
export declare function BarSegment({ activeIndex, color, index, scale, size, direction, roundedCorners, }: Props): JSX.Element;
export {};
//# sourceMappingURL=BarSegment.d.ts.map