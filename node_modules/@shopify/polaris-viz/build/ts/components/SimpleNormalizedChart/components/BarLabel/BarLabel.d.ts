/// <reference types="react" />
import type { Color, Direction } from '@shopify/polaris-viz-core';
import type { ComparisonMetricProps } from '../../../ComparisonMetric';
import type { LegendPosition } from '../../../../types';
export interface Props {
    activeIndex: number;
    index: number;
    label: string;
    value: string;
    color: Color;
    direction: Direction;
    legendPosition: LegendPosition;
    comparisonMetric?: ComparisonMetricProps | null;
}
export declare function BarLabel({ activeIndex, index, label, value, color, comparisonMetric, direction, legendPosition, }: Props): JSX.Element;
//# sourceMappingURL=BarLabel.d.ts.map