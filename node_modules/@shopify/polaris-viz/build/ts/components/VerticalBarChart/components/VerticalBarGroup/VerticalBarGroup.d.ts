/// <reference types="react" />
import type { StackedValues, YAxisOptions, Color, DataSeries } from '@shopify/polaris-viz-core';
import type { ScaleBand, ScaleLinear } from 'd3-scale';
import type { SortedBarChartData } from '../../../../types';
interface VerticalBarGroupProps {
    colors: Color[];
    data: DataSeries[];
    drawableHeight: number;
    gapWidth: number;
    id: string;
    labels: string[];
    sortedData: SortedBarChartData;
    stackedValues: StackedValues | null;
    xScale: ScaleBand<string>;
    yAxisOptions: Required<YAxisOptions>;
    yScale: ScaleLinear<number, number>;
    indexOffset?: number;
    areAllNegative?: boolean;
}
export declare function VerticalBarGroup({ colors, data, drawableHeight, gapWidth, id, indexOffset, labels, sortedData, stackedValues, xScale, yScale, yAxisOptions, areAllNegative, }: VerticalBarGroupProps): JSX.Element;
export {};
//# sourceMappingURL=VerticalBarGroup.d.ts.map