/// <reference types="react" />
import type { ScaleLinear } from 'd3-scale';
import type { Color } from '@shopify/polaris-viz-core';
import type { AccessibilitySeries } from '../../../VerticalBarChart/types';
export interface BarGroupProps {
    animationDelay: number;
    x: number;
    yScale: ScaleLinear<number, number>;
    width: number;
    drawableHeight: number;
    data: (number | null)[];
    colors: Color[];
    barGroupIndex: number;
    indexOffset: number;
    accessibilityData: AccessibilitySeries[];
    activeBarGroup: number;
    gapWidth: number;
    areAllNegative?: boolean;
    theme?: string;
}
export declare function BarGroup({ animationDelay, x, data, yScale, width, colors, drawableHeight, indexOffset, barGroupIndex, accessibilityData, activeBarGroup, gapWidth, theme, areAllNegative, }: BarGroupProps): JSX.Element;
//# sourceMappingURL=BarGroup.d.ts.map