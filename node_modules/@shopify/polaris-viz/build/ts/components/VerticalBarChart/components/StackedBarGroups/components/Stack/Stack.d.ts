/// <reference types="react" />
import type { ScaleLinear } from 'd3-scale';
import type { FormattedStackedSeries, StackedBarGapDirections } from '../../../../../../types';
interface StackProps {
    activeBarGroup: number;
    animationDelay: number;
    data: FormattedStackedSeries;
    gaps: {
        [key: number]: StackedBarGapDirections;
    };
    groupIndex: number;
    id: string;
    width: number;
    x: number | undefined;
    yScale: ScaleLinear<number, number>;
}
export declare function Stack({ activeBarGroup, animationDelay, data, gaps, groupIndex, id, width, x, yScale, }: StackProps): JSX.Element;
export {};
//# sourceMappingURL=Stack.d.ts.map