/// <reference types="react" />
import type { ScaleBand, ScaleLinear } from 'd3-scale';
import type { Color } from '@shopify/polaris-viz-core';
import type { AccessibilitySeries } from '../../../VerticalBarChart/types';
import type { StackedSeries } from '../../../../types';
interface StackedBarGroupsProps {
    accessibilityData: AccessibilitySeries[];
    activeBarGroup: number;
    colors: Color[];
    drawableHeight: number;
    gapWidth: number;
    id: string;
    labels: string[];
    stackedValues: StackedSeries[];
    xScale: ScaleBand<string>;
    yScale: ScaleLinear<number, number>;
}
export declare function StackedBarGroups({ accessibilityData, activeBarGroup, drawableHeight, gapWidth, id, labels, stackedValues, xScale, yScale, }: StackedBarGroupsProps): JSX.Element;
export {};
//# sourceMappingURL=StackedBarGroups.d.ts.map