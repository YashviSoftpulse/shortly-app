import type { Dispatch, SetStateAction } from 'react';
interface Props {
    allowLineWrap: boolean;
    labels: string[];
    targetWidth: number;
    onHeightChange?: Dispatch<SetStateAction<number>> | (() => void);
}
export declare function useLabels({ allowLineWrap, labels, onHeightChange, targetWidth, }: Props): {
    lines: import("../../../types").FormattedLine[][];
    containerHeight: number;
};
export {};
//# sourceMappingURL=useLabels.d.ts.map