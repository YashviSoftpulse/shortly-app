import type { BoundingRect, Dimensions } from '@shopify/polaris-viz-core';
import type { TooltipPositionOffset } from '../../TooltipWrapper';
import type { Margin } from '../../../types';
export declare const TOOLTIP_MARGIN = 20;
export declare const SCROLLBAR_WIDTH = 20;
export interface AlteredPositionProps {
    bandwidth: number;
    chartBounds: BoundingRect;
    currentX: number;
    currentY: number;
    isPerformanceImpacted: boolean;
    margin: Margin;
    position: TooltipPositionOffset;
    tooltipDimensions: Dimensions;
}
export interface AlteredPositionReturn {
    x: number;
    y: number;
}
export declare type AlteredPosition = (props: AlteredPositionProps) => AlteredPositionReturn;
export declare function getAlteredLineChartPosition(props: AlteredPositionProps): AlteredPositionReturn;
//# sourceMappingURL=getAlteredLineChartPosition.d.ts.map