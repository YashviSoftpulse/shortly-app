import type { LabelFormatter, LineChartDataSeriesWithDefaults } from '@shopify/polaris-viz-core';
import type { ReactNode } from 'react';
import type { RenderTooltipContentData } from '../../../types';
export interface Props {
    data: LineChartDataSeriesWithDefaults[];
    indexForLabels: number;
    renderTooltipContent: (data: RenderTooltipContentData) => ReactNode;
    seriesNameFormatter: LabelFormatter;
    hiddenIndexes?: number[];
}
export declare function useLineChartTooltipContent({ data, hiddenIndexes, indexForLabels, renderTooltipContent, seriesNameFormatter, }: Props): (activeIndex: number) => ReactNode;
//# sourceMappingURL=useLineChartTooltipContent.d.ts.map