import type { ReactNode } from 'react';
import type { Color, DataGroup, LabelFormatter } from '@shopify/polaris-viz-core';
import type { RenderTooltipContentData } from '../../../types';
export interface Props {
    data: DataGroup[];
    seriesColors: Color[];
    renderTooltipContent: (data: RenderTooltipContentData) => ReactNode;
    seriesNameFormatter: LabelFormatter;
}
export declare function useComboChartTooltipContent({ data, renderTooltipContent, seriesColors, seriesNameFormatter, }: Props): (activeIndex: number) => ReactNode;
//# sourceMappingURL=useComboChartTooltipContent.d.ts.map