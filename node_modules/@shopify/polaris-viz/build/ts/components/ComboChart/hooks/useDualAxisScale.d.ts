import type { ScaleLinear } from 'd3-scale';
import type { Axis } from '../types';
interface Props {
    doBothChartsContainMixedValues: boolean;
    doesOneChartContainAllNegativeValues: boolean;
    drawableHeight: number;
    primaryAxis: Axis;
    secondaryAxis: Axis;
    shouldPlaceZeroInMiddleOfChart: boolean;
    ticksBetweenZeroAndMax: number;
    yScale: ScaleLinear<number, number>;
}
export declare function useDualAxisScale({ doBothChartsContainMixedValues, doesOneChartContainAllNegativeValues, drawableHeight, primaryAxis, secondaryAxis, shouldPlaceZeroInMiddleOfChart, ticksBetweenZeroAndMax, yScale, }: Props): {
    barYScale: ScaleLinear<number, number, never>;
    lineYScale: ScaleLinear<number, number, never>;
    primaryYScale: ScaleLinear<number, number, never>;
    secondaryYScale: ScaleLinear<number, number, never>;
};
export {};
//# sourceMappingURL=useDualAxisScale.d.ts.map