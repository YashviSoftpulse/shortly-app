import type { Axis } from '../types';
export interface Props {
    leftTickWidth: number;
    primaryAxis: Axis;
    rightTickWidth: number;
    secondaryAxis: Axis;
    width: number;
}
export declare function useComboChartPositions({ leftTickWidth, primaryAxis, rightTickWidth, secondaryAxis, width, }: Props): {
    chartXPosition: number;
    drawableWidth: number;
    leftAxis: {
        x: number;
        labelX: number;
    };
    rightAxis: {
        x: number;
        labelX: number;
    };
};
//# sourceMappingURL=useComboChartPositions.d.ts.map