/// <reference types="react" />
import type { Color, DataGroup } from '@shopify/polaris-viz-core';
interface ComboLineChartProps {
    activeIndex: number | null;
    colors: Color[];
    data: DataGroup;
    drawableHeight: number;
    drawableWidth: number;
    indexOffset: number;
    theme: string;
    xScale: any;
    yScale: any;
}
export declare function ComboLineChart({ activeIndex, colors, data, drawableHeight, drawableWidth, indexOffset, theme, xScale, yScale, }: ComboLineChartProps): JSX.Element;
export {};
//# sourceMappingURL=ComboLineChart.d.ts.map