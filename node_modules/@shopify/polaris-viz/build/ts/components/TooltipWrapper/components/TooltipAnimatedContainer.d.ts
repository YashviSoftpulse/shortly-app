import type { ReactNode } from 'react';
import type { BoundingRect } from '@shopify/polaris-viz-core';
import type { Margin } from '../../../types';
import type { TooltipPositionOffset } from '../types';
import type { AlteredPosition } from '../utilities';
export interface TooltipAnimatedContainerProps {
    children: ReactNode;
    margin: Margin;
    activePointIndex: number;
    currentX: number;
    currentY: number;
    chartBounds: BoundingRect;
    getAlteredPosition?: AlteredPosition;
    position?: TooltipPositionOffset;
    id?: string;
    bandwidth?: number;
    chartDimensions?: BoundingRect;
}
export declare function TooltipAnimatedContainer({ activePointIndex, bandwidth, chartBounds, children, currentX, currentY, id, getAlteredPosition, margin, position, chartDimensions, }: TooltipAnimatedContainerProps): JSX.Element;
//# sourceMappingURL=TooltipAnimatedContainer.d.ts.map