import type { Dimensions } from '@shopify/polaris-viz-core';
import type { TooltipPositionOffset } from '../../TooltipWrapper';
import type { Margin } from '../../../types';
export interface AlteredPositionProps {
    bandwidth: number;
    chartBounds: {
        x: number;
        y: number;
        width: number;
        height: number;
    };
    currentX: number;
    currentY: number;
    isPerformanceImpacted: boolean;
    margin: Margin;
    position: TooltipPositionOffset;
    tooltipDimensions: Dimensions;
}
export interface AlteredPositionReturn {
    x: number;
    y: number;
}
export declare type AlteredPosition = (props: AlteredPositionProps) => AlteredPositionReturn;
export declare function getAlteredStackedAreaChartPosition(props: AlteredPositionProps): AlteredPositionReturn;
//# sourceMappingURL=getAlteredStackedAreaChartPosition.d.ts.map