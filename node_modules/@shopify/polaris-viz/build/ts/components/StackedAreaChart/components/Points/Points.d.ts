/// <reference types="react" />
import type { Series } from 'd3-shape';
import type { Color } from '@shopify/polaris-viz-core';
import type { ScaleLinear } from 'd3-scale';
import type { AnimatedCoordinate, GetXPosition } from '../../../../types';
interface PointsProps {
    activePointIndex: number | null;
    animatedCoordinates: AnimatedCoordinate[] | null;
    colors: Color[];
    getXPosition: GetXPosition;
    stackedValues: Series<{
        [key: string]: number;
    }, string>[];
    tooltipId: string;
    xScale: ScaleLinear<number, number>;
    yScale: ScaleLinear<number, number>;
}
export declare function Points({ activePointIndex, animatedCoordinates, colors, getXPosition, stackedValues, tooltipId, xScale, yScale, }: PointsProps): JSX.Element;
export {};
//# sourceMappingURL=Points.d.ts.map