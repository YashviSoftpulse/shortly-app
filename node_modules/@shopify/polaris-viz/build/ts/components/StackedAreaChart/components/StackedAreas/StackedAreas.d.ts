/// <reference types="react" />
import type { ScaleLinear } from 'd3-scale';
import type { Color } from '@shopify/polaris-viz-core';
import type { StackedSeries } from '../../../../types';
interface Props {
    colors: Color[];
    stackedValues: StackedSeries[];
    zeroLineValues: StackedSeries[];
    xScale: ScaleLinear<number, number>;
    yScale: ScaleLinear<number, number>;
    theme: string;
}
export declare function StackedAreas({ stackedValues, xScale, yScale, colors, theme, zeroLineValues, }: Props): JSX.Element;
export {};
//# sourceMappingURL=StackedAreas.d.ts.map