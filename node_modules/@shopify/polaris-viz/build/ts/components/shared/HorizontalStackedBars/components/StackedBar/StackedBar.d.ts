import type { Dispatch, SetStateAction } from 'react';
export interface StackedBarProps {
    zeroPosition: number;
    x: number;
    width: number;
    setActiveBarIndex: Dispatch<SetStateAction<number>>;
    seriesIndex: number;
    height: number;
    color: string;
    borderRadius: string;
    ariaLabel: string;
    animationDelay: number;
    activeBarIndex: number;
}
export declare function StackedBar({ animationDelay, activeBarIndex, ariaLabel, borderRadius, color, height, seriesIndex, setActiveBarIndex, width, x, zeroPosition, }: StackedBarProps): JSX.Element;
//# sourceMappingURL=StackedBar.d.ts.map