/// <reference types="react" />
import type { Shape, Color } from '@shopify/polaris-viz-core';
interface Props {
    activeIndex: number;
    index: number;
    label: string;
    shape: Shape;
    value: string;
    color?: Color;
    isComparison?: boolean;
    isHidden?: boolean;
    renderSeriesIcon?: () => React.ReactNode;
}
export declare function TooltipRow({ activeIndex, color, index, isComparison, isHidden, label, renderSeriesIcon, shape, value, }: Props): JSX.Element | null;
export {};
//# sourceMappingURL=TooltipRow.d.ts.map