import type { ReactNode } from 'react';
interface Props {
    children: ({ activeColorVisionIndex, }: {
        activeColorVisionIndex: number;
    }) => ReactNode;
    maxWidth: number;
    theme: string;
}
export declare function TooltipContentContainer({ children, maxWidth, theme }: Props): JSX.Element;
export {};
//# sourceMappingURL=TooltipContentContainer.d.ts.map