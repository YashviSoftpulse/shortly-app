import type { Dispatch, SetStateAction } from 'react';
import type { AnnotationPosition } from '../../types';
export interface AnnotationLabelProps {
    ariaLabel: string;
    hasContent: boolean;
    index: number;
    isVisible: boolean;
    label: string;
    position: AnnotationPosition;
    setActiveIndex: Dispatch<SetStateAction<number>>;
    tabIndex: number;
}
export declare function AnnotationLabel({ ariaLabel, hasContent, index, isVisible, label, position, setActiveIndex, tabIndex, }: AnnotationLabelProps): JSX.Element;
//# sourceMappingURL=AnnotationLabel.d.ts.map