import type { ScaleLinear } from 'd3-scale';
import type { Annotation, YAxisTick } from '../../../types';
import type { AnnotationPosition, OptionalDualAxisYAxis } from '../types';
export interface Props {
    annotations: Annotation[];
    axis: OptionalDualAxisYAxis;
    drawableWidth: number;
    ticks: YAxisTick[];
    yScale: ScaleLinear<number, number>;
}
export declare function useYAxisAnnotationPositions({ annotations, axis, drawableWidth, ticks, yScale, }: Props): {
    positions: AnnotationPosition[];
};
//# sourceMappingURL=useYAxisAnnotationPositions.d.ts.map