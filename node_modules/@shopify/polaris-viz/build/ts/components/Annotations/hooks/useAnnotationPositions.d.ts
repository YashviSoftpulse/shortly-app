import type { ScaleBand, ScaleLinear } from 'd3-scale';
import type { Annotation } from '../../../types';
import type { AnnotationPosition } from '../types';
export interface Props {
    annotations: Annotation[];
    axisLabelWidth: number;
    dataIndexes: {
        [key: string]: string;
    };
    drawableWidth: number;
    isShowingAllAnnotations: boolean;
    onHeightChange: (height: number) => void;
    xScale: ScaleLinear<number, number> | ScaleBand<string>;
}
export declare function useAnnotationPositions({ annotations, axisLabelWidth, dataIndexes, drawableWidth, isShowingAllAnnotations, onHeightChange, xScale, }: Props): {
    hiddenAnnotationsCount: number;
    positions: AnnotationPosition[];
    rowCount: number;
};
//# sourceMappingURL=useAnnotationPositions.d.ts.map