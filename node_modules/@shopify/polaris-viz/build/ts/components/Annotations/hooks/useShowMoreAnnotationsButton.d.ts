import type { AnnotationPosition } from '../types';
interface Props {
    isShowingAllAnnotations: boolean;
    positions: AnnotationPosition[];
}
export declare function useShowMoreAnnotationsButton({ isShowingAllAnnotations, positions, }: Props): {
    rowCount: number;
    totalRowHeight: number;
};
export {};
//# sourceMappingURL=useShowMoreAnnotationsButton.d.ts.map