'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var isGradientType = require('./isGradientType.js');

function getGradientFromColor(color) {
  return isGradientType.isGradientType(color) ? color : [{
    color,
    offset: 0
  }, {
    color,
    offset: 1
  }];
}

exports.getGradientFromColor = getGradientFromColor;
