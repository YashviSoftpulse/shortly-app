'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var constants = require('../constants.js');

function getColorVisionStylesForActiveIndex({
  activeIndex,
  index,
  fadedOpacity = constants.COLOR_VISION_FADED_OPACITY
}) {
  const activeOpacity = activeIndex === index ? constants.COLOR_VISION_ACTIVE_OPACITY : fadedOpacity;
  return {
    opacity: activeIndex === -1 ? constants.COLOR_VISION_ACTIVE_OPACITY : activeOpacity,
    transition: 'opacity 100ms ease'
  };
}

exports.getColorVisionStylesForActiveIndex = getColorVisionStylesForActiveIndex;
