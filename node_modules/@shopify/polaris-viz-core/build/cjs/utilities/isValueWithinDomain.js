'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

function isValueWithinDomain(value, domain) {
  return value >= Math.min(...domain) && value <= Math.max(...domain);
}

exports.isValueWithinDomain = isValueWithinDomain;
