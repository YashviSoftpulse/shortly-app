import type { SpringConfig } from '@react-spring/web';
interface Props {
    animationDelay?: number;
    shouldAnimate?: boolean;
    mountedSpringConfig?: SpringConfig;
    unmountedSpringConfig?: SpringConfig;
}
export declare function useSpringConfig({ animationDelay, shouldAnimate, mountedSpringConfig, unmountedSpringConfig, }: Props): {
    config: Partial<import("@react-spring/core/dist/declarations/src/AnimationConfig").AnimationConfig> | undefined;
    default: {
        immediate: boolean;
    };
    delay: number;
    onRest: () => boolean;
};
export {};
//# sourceMappingURL=useSpringConfig.d.ts.map